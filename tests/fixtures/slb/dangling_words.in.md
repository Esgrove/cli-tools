# Dangling words

A line that ends with one of these words leaves its clause unfinished,
so the capitalized name on the next line continues it and the two lines are joined.
A line that ends with any other word before a capitalized one is left alone.

The tests pass on every agent in the pool
Linux agents finish first.

Every job runs on an agent from the
Linux pool.

The installer asks for a
GitHub token first.

The release adds an
ARM build for every platform.

The script prints the version of
Python it found.

The job pushes the image to
Docker Hub after the tests pass.

The binary is signed in
Xcode before the upload.

The tests run on
Windows and macOS.

The package is built for
Debian and Fedora.

The archive is compressed with
Zstandard by default.

The report is generated by
Jenkins after every build.

The tokens are read from
Vault when the job starts.

The mirror is hosted at
GitLab for the other teams.

The logs are shipped into
Elasticsearch every minute.

The image bundles the compiler and
CMake for the native builds.

The config can be written in TOML or
YAML with the same keys.

The tool reads every format but
XML is the slowest one.

The default shell is
Bash on every agent.

The supported platforms are
Linux and macOS.

The report can be
HTML or plain text.

The previous default was
Python two.

The old builds were
Jenkins freestyle jobs.

The docs mention that
Gradle caches the build outputs.

The job picks the runner which
Kubernetes schedules first.

The pipeline uses this
Docker image for every stage.

The plugin supports these
IDE versions only.

The build skips those
Go modules that are vendored.

The service writes its
JSON logs to standard output.

The users keep their
SSH keys in the agent.

The file is saved as
UTF-8 without a byte order mark.

The flag is not
POSIX compatible on every shell.

The build needs no
Java runtime at all.

The tool works with any
Git hosting service.

The job runs all
Python tests in parallel.

The script tags each
Docker image with the commit hash.

The release notes list every
API change since the last version.

Both jobs use the same
Node version as the local setup.

The new parser is faster than
ANTLR on large files.

The upload is very
CPU heavy on the small agents.

The report shows more
GC pauses than expected.

The cache helps most
Rust builds by a wide margin.
