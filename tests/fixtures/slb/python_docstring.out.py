"""Module docstring that is wrapped in the middle of a clause."""

# needed for paths
import os


def parse(path: str) -> dict:
    """Parse the file at the given path and return the parsed mapping.
    Missing files raise.

    Args:
        path: the file path that is
            documented here.

    Returns:
        The mapping.
    """
    return {}  # type: ignore
