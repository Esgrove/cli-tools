"""Code examples written inside comments stay exactly as the author wrote them."""

# Quick start for the REPL:
# from pathlib import Path; from typing import Optional; import sys


def main() -> None:
    # Debug the parser by running the script with the verbose flag, and the output lists every token it reads.
    # The report goes to the terminal.
    print("done")
